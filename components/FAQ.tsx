'use client'
import { useState } from 'react'
const qs=[{q:'Is this free?',a:'There are free credits; additional usage costs a small $MAGI fee with a tiny burn.'},{q:'How do creators get paid?',a:'Tips, boosts, and royalties split on-chain to creators, hunters, and stakers.'},{q:'Which platforms are supported?',a:'YouTube, Twitch, Kick, X Spaces initially; more via community votes.'},{q:'Do you handle copyright?',a:'Creators opt-in and license their clips. We honor takedowns and keep a moderation queue.'}]
export default function FAQ(){const [open,setOpen]=useState<number|null>(0);return (<section id="faq" className="container pb-16"><h2 className="text-2xl md:text-3xl font-bold">FAQ</h2><div className="mt-6 space-y-4">{qs.map((item,idx)=>(<div key={idx} className="card"><button onClick={()=>setOpen(open===idx?null:idx)} className="w-full text-left px-5 py-4 font-medium flex items-center justify-between">{item.q}<span className="text-white/60">{open===idx?'–':'+'}</span></button>{open===idx && <div className="px-5 pb-4 text-white/70 text-sm">{item.a}</div>}</div>))}</div></section>) }
