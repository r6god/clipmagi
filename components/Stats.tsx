'use client'
import { useEffect, useState } from 'react'
function useCounter(target:number,duration=800){const [v,setV]=useState(0);useEffect(()=>{let s:any=null;const step=(t:number)=>{if(s===null)s=t;const p=Math.min(1,(t-s)/duration);setV(Math.floor(p*target));if(p<1)requestAnimationFrame(step)};requestAnimationFrame(step)},[target,duration]);return v}
export default function Stats(){const c1=useCounter(45);const c2=useCounter(1080);const c3=useCounter(30);return (<section className="container pb-12 grid sm:grid-cols-3 gap-6"><div className="card p-6 text-center"><div className="text-4xl font-extrabold">{c1}%</div><p className="text-white/60 text-sm mt-1">Avg retention lift</p></div><div className="card p-6 text-center"><div className="text-4xl font-extrabold">{c2}p</div><p className="text-white/60 text-sm mt-1">1080×1920 shorts</p></div><div className="card p-6 text-center"><div className="text-4xl font-extrabold">{c3}s</div><p className="text-white/60 text-sm mt-1">Clip length sweet spot</p></div></section>) }
